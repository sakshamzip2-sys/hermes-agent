# Attribution

These skills are ported from Anthropic's open-source `knowledge-work-plugins`
repository (https://github.com/anthropics/knowledge-work-plugins), licensed
under the Apache License 2.0 (see LICENSE in this directory).

Ported into the OpenComputer v2 bundled skill pool as the `knowledge-work`
namespace. Each skill's frontmatter `name` is prefixed `kw-<plugin>-` to keep
names unique in the shared pool; directory structure and skill bodies are
otherwise preserved so the in-skill relative references still resolve. Em
dashes were normalized to hyphens per the project style rule.
